# Constellation Graph

An interactive constellation visualization of survey responses — each question is a hub, each respondent's answer is a satellite orbiting it.

## Development

```
npm install
npm run dev
```
